<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.lee</groupId>
        <artifactId>DLect-all</artifactId>
        <version>2.1-2</version>
    </parent>
    <groupId>org.lee</groupId>
    <artifactId>DLect-Release</artifactId>
    <version>2.1-2</version>

    <name>DLect Release Build</name>

    <properties>
        <release.jar.combined>DLect-combined</release.jar.combined>
        <release.jar>DLect</release.jar>
        <app.main.class>DLect</app.main.class>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>jsr305</artifactId>
            <version>2.0.0</version>
            <type>jar</type>
        </dependency>
        <dependency>
            <groupId>javax.inject</groupId>
            <artifactId>javax.inject</artifactId>
            <version>1</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>DLectUpdater</artifactId>
            <version>1.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>DLect-Standalone</artifactId>
            <version>2.1-2</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.7</version>
                <executions>
                    <execution>
                        <id>copy</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>DLectUpdater</artifactId>
                                    <version>1.0</version>
                                    <type>jar</type>
                                    <outputDirectory>${project.build.outputDirectory}/updater</outputDirectory>
                                    <destFileName>updater.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>2.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <outputFile>${project.build.directory}/${release.jar.combined}.jar</outputFile>
                            <minimizeJar>false</minimizeJar>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>org/apache/commons/logging/impl/AvalonLogger*</exclude>
                                        <exclude>org/apache/commons/logging/impl/Log4JLogger*</exclude>
                                        <exclude>org/apache/commons/logging/impl/LogKitLogger*</exclude>
                                        <exclude>org/apache/commons/logging/impl/ServletContextCleaner*</exclude>
                                        <exclude>com/googlecode/mp4parser/util/Logger</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <manifestEntries>
                                        <Main-Class>${app.main.class}</Main-Class>
                                        <X-Test>MAIN CLASS IS: '${app.main.class}'</X-Test>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>com.github.wvengen</groupId>
                <artifactId>proguard-maven-plugin</artifactId>
                <version>2.0.6</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>proguard</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <proguardVersion>4.8</proguardVersion>
                    <obfuscate>false</obfuscate>
                    <injar>/${release.jar.combined}.jar</injar>
                    <outjar>/${release.jar}.jar</outjar>
                    <outputDirectory>${project.build.directory}</outputDirectory>
                    <proguardInclude>${basedir}/proguard.conf</proguardInclude>
                    <options>
                        <option>-allowaccessmodification</option>
                        <option>-optimizations !code/allocation/variable</option>
                        <option>-keep public class ${app.main.class} {
                            public static void main(java.lang.String[]);
                            }</option>
                        <option>-keep class org.lee.echo360.providers.implementers.**{*;}</option>
                        <option>-keep class org.lee.echo360.model.**{*;}</option>
                        <option>-keep class org.jaudiotagger.**{*;}</option>
                        <option>-keep class com.coremedia.iso.**{*;}</option>
                        <option>-keep class org.apache.commons.logging.impl.LogFactoryImpl{*;}</option>
                        <option>-keep class org.apache.commons.logging.impl.SimpleLog{*;}</option>
                        <option>-keep class org.apache.commons.logging.impl.Jdk14Logger{*;}</option>
                        <option>-keep class org.apache.commons.lang3.text.translate.NumericEntityUnescaper{*;}</option>
                        <option>-keep class org.apache.commons.lang3.text.translate.NumericEntityUnescaper$OPTION{*;}</option>
                        <option>-dontwarn com.googlecode.mp4parser.util.AndroidLogger</option>
                    </options>
                    <includeDependency>false</includeDependency>
                    <libs>
                        <lib>${java.home}/lib/rt.jar</lib>
                        <lib>${java.home}/lib/jsse.jar</lib>
                        <lib>${java.home}/lib/jce.jar</lib>
                    </libs>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>net.sf.proguard</groupId>
                        <artifactId>proguard-base</artifactId>
                        <version>4.8</version>
                        <scope>runtime</scope>
                    </dependency>
                </dependencies>
            </plugin>

            <plugin>
                <groupId>com.github.goldin</groupId>
                <artifactId>copy-maven-plugin</artifactId>
                <version>0.2.5</version>
                <executions>
                    <execution>
                        <id>create-archive</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <resource>
                                <targetPath>${project.build.directory}</targetPath>
                                <destFileName>${project.build.finalName}.jar</destFileName>
                                <file>${project.build.directory}/${release.jar}.jar</file>
                            </resource>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
